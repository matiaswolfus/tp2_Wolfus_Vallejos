const sumar = (num1, num2) => num1 + num2;

const restar = (num1, num2) => num1 - num2;

const pi = 3.1416;

const multiplicarPi = num1 => num1 * pi;

function dividir(num1, num2)
{
    return num1 / num2;
}  

export{ multiplicarPi,sumar, restar, dividir}